abc070_b.ex
##############################
##############################
##############################
##############################
##############################
##############################
##############################
##############################
defmodule Main do
  def main do
    [a,b,c,d] =
      IO.read(:line)
      |> String.trim
      |> String.split(" ")
      |>Enum.map(fn x->String.to_integer(x)end)
    t=max(min(b,d)-max(a,c),0)
    IO.puts(t)
  end
end
##############################
